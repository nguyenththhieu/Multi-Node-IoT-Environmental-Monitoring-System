; ============================================================================
; ĐỒ ÁN 2 - NODE MASTER
; Cấu hình PlatformIO cho ESP32 DevKit / NodeMCU LuaNode32
; ============================================================================
[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
upload_speed = 921600

; Không cần thư viện ngoài: dùng WiFi.h và WebServer.h có sẵn trong ESP32 Arduino core
; (JSON được dựng thủ công bằng chuỗi String để giảm phụ thuộc thư viện, theo đúng yêu cầu)
lib_deps =
